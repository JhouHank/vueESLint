// 用來配置 JavaScript 專案的設定檔案，它通常用在沒有 TypeScript 的專案中，
// 但你希望使用 TypeScript 的一些功能或者進行代碼導航。
{
  "compilerOptions": {
    // 當你使用 @/* 這個模塊路徑時，實際上是指向 ./src/* 的路徑。
    // 例如，如果你有一個檔案想引入 ./src/components/Button.js，
    // 你可以使用 import Button from '@/components/Button' 而不是寫全路徑。
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  // 指定了哪些目錄應該被排除在編譯的過程中。
  "exclude": ["node_modules", "dist"]
}
